#include "pin.hpp"
#include "block/header/header.hpp"

std::optional<GridPin> GridPin::checkpoint(){
    // UPDATE GRID PIN HERE (HARD-CODED CHAIN CHECKPOINT)
    // static Header h("<insert snapshot here>");
    // return {Batchslot(0),h};
    return {};
};
